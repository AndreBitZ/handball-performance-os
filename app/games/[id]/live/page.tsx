'use client'

import Link from 'next/link'
import { useEffect, useMemo, useState } from 'react'
import { ArrowLeft, Pause, Play, RotateCcw, ArrowRightLeft } from 'lucide-react'
import LiveCoding, { type LiveCodingEvent } from '../../live-coding'
import { db } from '../../../../src/lib/storage/db'
import { calculatePlayerSeconds, HALF_SECONDS } from '../../../../src/lib/match/minutes'
import { requestPersistentStorage, isPersistentStorageGranted, saveMatchRecoverySnapshot } from '../../../../src/lib/storage/resilience'
import type { Match, MatchSquad, Player, MatchClockState } from '../../../../src/lib/storage/types'

const ON_COURT=7
type PlayerWithSquad=Player & Pick<MatchSquad,'shirtNumber'|'position'|'starter'>
const uid=()=>crypto.randomUUID(), nowIso=()=>new Date().toISOString()
const elapsedFromClock=(clock:MatchClockState,now=Date.now())=>Math.min(HALF_SECONDS,clock.elapsedSeconds+(clock.running&&clock.lastStartedAt?Math.max(0,(now-Date.parse(clock.lastStartedAt))/1000):0))

export default function LiveMatchPage({params}:{params:{id:string}}){
 const [match,setMatch]=useState<Match|null>(null),[players,setPlayers]=useState<PlayerWithSquad[]>([]),[onCourt,setOnCourt]=useState<string[]>([])
 const [period,setPeriod]=useState<1|2>(1),[seconds,setSeconds]=useState(0),[running,setRunning]=useState(false),[startedPeriod,setStartedPeriod]=useState(false)
 const [savedCount,setSavedCount]=useState(0),[minutes,setMinutes]=useState<Record<string,number>>({}),[subOut,setSubOut]=useState<string|null>(null),[error,setError]=useState<string|null>(null),[persistent,setPersistent]=useState(false)
 useEffect(()=>{void requestPersistentStorage().then(async()=>setPersistent(await isPersistentStorageGranted()))},[])
 useEffect(()=>{let active=true;async function load(){if(!db)return;const database=db,current=await database.matches.get(params.id);if(!current)return;const squad=await database.matchSquads.where('matchId').equals(params.id).toArray(),ids=squad.map(x=>x.playerId),loaded=ids.length?await database.players.where('id').anyOf(ids).toArray():[],byId=new Map(loaded.map(p=>[p.id,p])),mapped:PlayerWithSquad[]=squad.flatMap(item=>{const p=byId.get(item.playerId);return p?[{...p,shirtNumber:item.shirtNumber??p.shirtNumber,position:item.position??p.position,starter:item.starter}]:[]}).sort((a,b)=>(a.shirtNumber??999)-(b.shirtNumber??999)),intervals=await database.playerIntervals.where('matchId').equals(params.id).toArray(),events=await database.events.where('matchId').equals(params.id).toArray(),clocks=await database.clockStates.where('matchId').equals(params.id).toArray();if(!active)return;const currentPeriod:1|2=clocks.some(c=>c.period===2)?2:intervals.some(i=>i.period===2)?2:1,clock=clocks.find(c=>c.period===currentPeriod),elapsed=clock?elapsedFromClock(clock):0,open=intervals.filter(i=>i.period===currentPeriod&&i.endSeconds===undefined);setMatch(current);setPlayers(mapped);setSavedCount(events.length);setPeriod(currentPeriod);setOnCourt(open.map(i=>i.playerId).slice(0,ON_COURT));setStartedPeriod(open.length>0||!!clock);setSeconds(elapsed);setRunning(!!clock?.running)}void load();return()=>{active=false}},[params.id])
 useEffect(()=>{if(!running)return;const t=window.setInterval(()=>setSeconds(v=>Math.min(HALF_SECONDS,v+0.25)),250);return()=>window.clearInterval(t)},[running])
 useEffect(()=>{if(!db||!match||!running)return;const database=db,id=window.setInterval(async()=>{const clock=await database.clockStates.where('[matchId+period]').equals([match.id,period]).first();if(clock?.running){await database.clockStates.update(clock.id,{elapsedSeconds:seconds,updatedAt:nowIso()});await saveMatchRecoverySnapshot(database,match.id)}},5000);return()=>window.clearInterval(id)},[match,period,running,seconds])
 useEffect(()=>{if(!db||!match)return;const database=db;let cancelled=false;const refresh=async()=>{const all=await database.playerIntervals.where('matchId').equals(match.id).toArray();if(cancelled)return;setMinutes(calculatePlayerSeconds(all,period,seconds))};void refresh();return()=>{cancelled=true}},[seconds,period,match])
 useEffect(()=>{if(running&&seconds>=HALF_SECONDS)void pauseClock()},[seconds,running])
 const displayTime=useMemo(()=>`${Math.floor(seconds/60).toString().padStart(2,'0')}:${Math.floor(seconds%60).toString().padStart(2,'0')}`,[seconds]),courtPlayers=useMemo(()=>players.filter(p=>onCourt.includes(p.id)),[players,onCourt]),benchPlayers=useMemo(()=>players.filter(p=>!onCourt.includes(p.id)),[players,onCourt])
 async function writeClock(elapsedSeconds:number,isRunning:boolean,lastStartedAt?:string){if(!db||!match)return;const database=db,existing=await database.clockStates.where('[matchId+period]').equals([match.id,period]).first(),payload={elapsedSeconds:Math.min(HALF_SECONDS,Math.max(0,elapsedSeconds)),running:isRunning,lastStartedAt,updatedAt:nowIso()};if(existing)await database.clockStates.update(existing.id,payload);else await database.clockStates.add({id:uid(),matchId:match.id,period,...payload});await saveMatchRecoverySnapshot(database,match.id)}
 async function startClock(){await writeClock(seconds,true,nowIso());setRunning(true);setStartedPeriod(true)}
 async function pauseClock(){if(!db||!match)return;const database=db,clock=await database.clockStates.where('[matchId+period]').equals([match.id,period]).first(),elapsed=clock?elapsedFromClock(clock):seconds;await writeClock(elapsed,false);setSeconds(elapsed);setRunning(false)}
 async function resetClock(){await writeClock(0,false);setSeconds(0);setRunning(false)}
 async function startPeriod(){if(!db||!players.length)return;setError(null);try{const database=db,existing=await database.playerIntervals.where('matchId').equals(params.id).toArray(),open=existing.filter(i=>i.period===period&&i.endSeconds===undefined);let lineup=open.map(i=>i.playerId);if(!lineup.length){lineup=players.filter(p=>p.starter).slice(0,ON_COURT).map(p=>p.id);if(lineup.length<ON_COURT)lineup=[...lineup,...players.filter(p=>!lineup.includes(p.id)).slice(0,ON_COURT-lineup.length).map(p=>p.id)];const now=nowIso();await database.playerIntervals.bulkAdd(lineup.map(playerId=>({id:uid(),matchId:params.id,playerId,period,startSeconds:0,createdAt:now,updatedAt:now})))}setOnCourt(lineup.slice(0,ON_COURT));await startClock()}catch{setError('Não foi possível iniciar a parte.')}}
 async function finishPeriod(){if(!db)return;await pauseClock();const database=db,all=await database.playerIntervals.where('matchId').equals(params.id).toArray(),open=all.filter(i=>i.period===period&&i.endSeconds===undefined),now=nowIso();for(const i of open)await database.playerIntervals.update(i.id,{endSeconds:seconds,updatedAt:now});await saveMatchRecoverySnapshot(database,params.id);setStartedPeriod(false);if(period===1){setPeriod(2);setSeconds(0);setSubOut(null)}}
 async function substitute(playerIn:string){if(!db||!subOut||playerIn===subOut||onCourt.length!==ON_COURT||!startedPeriod)return;setError(null);try{const database=db,all=await database.playerIntervals.where('matchId').equals(params.id).toArray(),out=all.find(i=>i.period===period&&i.playerId===subOut&&i.endSeconds===undefined);if(!out)throw new Error();const now=nowIso();await database.playerIntervals.update(out.id,{endSeconds:seconds,updatedAt:now});await database.playerIntervals.add({id:uid(),matchId:params.id,playerId:playerIn,period,startSeconds:seconds,createdAt:now,updatedAt:now});await saveMatchRecoverySnapshot(database,params.id);setOnCourt(v=>v.map(id=>id===subOut?playerIn:id));setSubOut(null)}catch{setError('Não foi possível efetuar a substituição.')}}
 async function handleEvent(event:LiveCodingEvent){if(!db)return;setError(null);try{const database=db;await database.events.add({id:event.id,matchId:params.id,timestampSeconds:event.timestampSeconds,period:event.period,type:event.action,playerId:event.playerId,zone:event.context?.zone,distance:event.context?.distance,shotType:event.context?.shotType,result:event.context?.result,attackPhase:event.context?.attackPhase,createdAt:nowIso()});await saveMatchRecoverySnapshot(database,params.id);setSavedCount(v=>v+1)}catch{setError('Não foi possível guardar o evento.')}}
 if(!match)return <main className="content standalonePage"><p>A carregar jogo…</p></main>
 return <main className="content standalonePage"><header className="topbar"><div><Link href={`/games/${match.id}`} className="navItem" style={{display:'inline-flex',padding:0,marginBottom:12}}><ArrowLeft size={16}/> Jogo</Link><p className="eyebrow">LIVE MATCH</p><h1>{match.opponentName}</h1><p>{period}.ª parte · {savedCount} eventos · {courtPlayers.length}/7 em campo {persistent?'· armazenamento persistente ativo':'· armazenamento persistente não garantido'}</p></div><div className="heroBadge" aria-live="polite">{displayTime}</div></header><section className="hero"><div><p className="eyebrow">CONTROLO DO JOGO</p><h2>{period}.ª parte</h2><p>{startedPeriod?'Parte iniciada. O relógio e snapshots de recuperação são guardados localmente.':'Inicia a parte para criar os primeiros intervalos.'}</p></div><div style={{display:'flex',gap:8,flexWrap:'wrap',justifyContent:'flex-end'}}>{!startedPeriod?<button onClick={()=>void startPeriod()} disabled={!players.length}><Play size={16}/> Iniciar parte</button>:<button onClick={()=>void(running?pauseClock():startClock())}>{running?<Pause size={16}/>:<Play size={16}/>} {running?'Pausar':'Retomar'}</button>}<button onClick={()=>void resetClock()}><RotateCcw size={16}/> Reiniciar relógio</button>{startedPeriod&&<button onClick={()=>void finishPeriod()} disabled={seconds<HALF_SECONDS}>{period===1?'Fechar 1.ª parte':'Fechar jogo'}</button>}</div></section>{error&&<p role="alert" style={{color:'crimson',marginTop:12}}>{error}</p>}<section className="section"><div className="sectionHeader"><div><p className="eyebrow">7 EM CAMPO</p><h2>{courtPlayers.length}/{ON_COURT}</h2></div><ArrowRightLeft size={20}/></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:8}}>{courtPlayers.map(p=><button key={p.id} onClick={()=>setSubOut(p.id)} aria-pressed={subOut===p.id} style={{minHeight:62}}><strong>#{p.shirtNumber??'—'} {p.displayName}</strong><br/><small>{p.position??'—'} · {((minutes[p.id]??0)/60).toFixed(1)} min</small></button>)}</div>{subOut&&<p style={{marginTop:12}}>A sair: <strong>{players.find(p=>p.id===subOut)?.displayName}</strong>. Escolhe quem entra.</p>}</section><section className="section"><div className="sectionHeader"><div><p className="eyebrow">BANCO</p><h2>Substituições</h2></div></div><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(150px,1fr))',gap:8}}>{benchPlayers.map(p=><button key={p.id} onClick={()=>void substitute(p.id)} disabled={!subOut||!startedPeriod} style={{minHeight:62}}><strong>#{p.shirtNumber??'—'} {p.displayName}</strong><br/><small>{p.position??'—'} · {((minutes[p.id]??0)/60).toFixed(1)} min</small></button>)}</div>{!benchPlayers.length&&<p>Não existem jogadores no banco.</p>}</section><LiveCoding players={courtPlayers} period={period} timestampSeconds={period===1?seconds:HALF_SECONDS+seconds} onEvent={handleEvent}/>{!players.length&&<section className="section"><p>Este jogo ainda não tem convocados. Adiciona jogadores ao jogo antes de fazer live coding.</p></section>}</main>

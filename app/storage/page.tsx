'use client'

import { useEffect, useRef, useState } from 'react'
import { openProjectFolder, readProjectFile, writeProjectFile, listProjectFiles, PROJECT_FOLDERS, type ProjectFolderHandle } from '@/lib/storage/local-project'
import { loadProjectFolderHandle, requestProjectFolderPermission, saveProjectFolderHandle } from '@/lib/storage/project-session'
import { compareDatabaseRestore, previewDatabaseRestore, restoreDatabaseMerge, rollbackDatabaseRestore, saveDatabaseSnapshot, verifyDatabaseSnapshot, type DatabaseRestoreDiff } from '@/lib/storage/project-snapshot'
import { createProjectArchive, downloadProjectArchive, restoreProjectArchive } from '@/lib/storage/project-archive'

type RestorePreview = { filename: string; exportedAt: string; totalRows: number; tableCounts: Record<string, number> }

export default function StoragePage() {
  const [folder, setFolder] = useState<ProjectFolderHandle | null>(null)
  const [mode, setMode] = useState<'filesystem' | 'indexeddb' | null>(null)
  const [status, setStatus] = useState('Nenhum armazenamento local ligado')
  const [testing, setTesting] = useState(false); const [backingUp, setBackingUp] = useState(false); const [verifyingBackup, setVerifyingBackup] = useState(false); const [previewingRestore, setPreviewingRestore] = useState(false); const [comparingRestore, setComparingRestore] = useState(false); const [restoringDatabase, setRestoringDatabase] = useState(false); const [rollingBack, setRollingBack] = useState(false); const [archiving, setArchiving] = useState(false); const [importingArchive, setImportingArchive] = useState(false)
  const [lastBackup, setLastBackup] = useState<string | null>(null); const [restorePreview, setRestorePreview] = useState<RestorePreview | null>(null); const [restoreDiff, setRestoreDiff] = useState<DatabaseRestoreDiff | null>(null); const [safetyBackups, setSafetyBackups] = useState<string[]>([]); const [selectedSafetyBackup, setSelectedSafetyBackup] = useState(''); const [restoring, setRestoring] = useState(true)
  const archiveInputRef = useRef<HTMLInputElement>(null)

  async function refreshSafetyBackups(handle: ProjectFolderHandle) { try { const files = await listProjectFiles(handle, 'database'); const backups = files.filter((name) => name.startsWith('pre-restore-') && name.endsWith('.json')).sort().reverse(); setSafetyBackups(backups); setSelectedSafetyBackup((current) => current && backups.includes(current) ? current : backups[0] ?? '') } catch { setSafetyBackups([]); setSelectedSafetyBackup('') } }
  useEffect(() => { let active = true; async function restoreFolder() { try { const savedHandle = await loadProjectFolderHandle(); if (!savedHandle || !active) return; const granted = await requestProjectFolderPermission(savedHandle); if (!active) return; if (granted) { setFolder(savedHandle); const isLocal = typeof savedHandle === 'object' && 'kind' in savedHandle && savedHandle.kind === 'indexeddb'; setMode(isLocal ? 'indexeddb' : 'filesystem'); setStatus(isLocal ? 'Armazenamento local Safari recuperado.' : 'Pasta local recuperada.'); await refreshSafetyBackups(savedHandle) } else setStatus('Pasta guardada, mas a permissão de acesso precisa de ser renovada.') } catch (error) { if (active) setStatus(error instanceof Error ? error.message : 'Não foi possível recuperar o armazenamento local.') } finally { if (active) setRestoring(false) } } void restoreFolder(); return () => { active = false } }, [])
  async function chooseFolder() { try { setStatus('A preparar armazenamento local…'); const project = await openProjectFolder(); await saveProjectFolderHandle(project.handle); setFolder(project.handle); setMode(project.mode); setRestorePreview(null); setRestoreDiff(null); await refreshSafetyBackups(project.handle); setStatus(project.mode === 'indexeddb' ? 'Local-first ativo: Safari está a usar IndexedDB para guardar o projeto.' : `Pasta ligada e guardada: ${project.handle instanceof Object && 'name' in project.handle ? project.handle.name : 'projeto'}`) } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível ligar o armazenamento local.') } }
  async function reconnectFolder() { if (!folder) return; try { setStatus('A validar armazenamento…'); const granted = await requestProjectFolderPermission(folder); if (granted) await refreshSafetyBackups(folder); setStatus(granted ? (mode === 'indexeddb' ? 'Armazenamento local Safari confirmado.' : 'Acesso à pasta confirmado.') : 'Acesso ao armazenamento não autorizado.') } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível validar o armazenamento.') } }
  async function testStorage() { if (!folder) return; setTesting(true); try { const filename = '.storage-test.txt'; const value = `Handball Performance OS storage test — ${new Date().toISOString()}`; await writeProjectFile(folder, 'database', filename, value); const readBack = await readProjectFile(folder, 'database', filename); setStatus(readBack === value ? `Teste concluído: leitura e escrita funcionam${mode === 'indexeddb' ? ' no armazenamento local Safari' : ''}.` : 'Teste falhou: conteúdo diferente') } catch (error) { setStatus(error instanceof Error ? error.message : 'O teste falhou.') } finally { setTesting(false) } }
  async function createDatabaseBackup() { if (!folder) return; setBackingUp(true); try { setStatus('A criar backup da base de dados…'); const filename = await saveDatabaseSnapshot(folder); setLastBackup(new Date().toLocaleString('pt-PT')); setRestorePreview(null); setRestoreDiff(null); await refreshSafetyBackups(folder); setStatus(`Backup criado com sucesso: database/${filename}`) } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível criar o backup da base de dados.') } finally { setBackingUp(false) } }
  async function verifyDatabaseBackup() { if (!folder) return; setVerifyingBackup(true); try { setStatus('A verificar o backup local…'); const result = await verifyDatabaseSnapshot(folder); const totalRows = Object.values(result.tableCounts).reduce((sum, count) => sum + count, 0); setLastBackup(new Date(result.exportedAt).toLocaleString('pt-PT')); setStatus(`Backup válido: ${result.filename} — ${totalRows} registos verificados`) } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível verificar o backup.') } finally { setVerifyingBackup(false) } }
  async function previewRestore() { if (!folder) return; setPreviewingRestore(true); try { setStatus('A preparar pré-visualização do restauro…'); const preview = await previewDatabaseRestore(folder); setRestorePreview(preview); setRestoreDiff(null); setStatus(`Pré-visualização pronta: ${preview.totalRows} registos no backup`) } catch (error) { setRestorePreview(null); setRestoreDiff(null); setStatus(error instanceof Error ? error.message : 'Não foi possível preparar o restauro.') } finally { setPreviewingRestore(false) } }
  async function compareRestore() { if (!folder) return; setComparingRestore(true); try { setStatus('A comparar o backup com a base de dados atual…'); const diff = await compareDatabaseRestore(folder); setRestoreDiff(diff); setStatus(`Comparação concluída: ${diff.added} novos, ${diff.removed} removidos, ${diff.changed} alterados`) } catch (error) { setRestoreDiff(null); setStatus(error instanceof Error ? error.message : 'Não foi possível comparar o backup.') } finally { setComparingRestore(false) } }
  async function restoreDatabase() { if (!folder || !restoreDiff) return; if (!window.confirm('Restaurar o backup em modo seguro? Os dados atuais exclusivos serão preservados e será criado um backup de segurança antes da operação.')) return; setRestoringDatabase(true); try { setStatus('A criar backup de segurança e a restaurar…'); const result = await restoreDatabaseMerge(folder); await refreshSafetyBackups(folder); setSelectedSafetyBackup(result.safetyBackup); setStatus(`Restauro concluído: ${result.restoredRows} registos aplicados. ${result.preservedRows} preservados. Backup de segurança: database/${result.safetyBackup}`); setRestoreDiff(await compareDatabaseRestore(folder)) } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível restaurar a base de dados.') } finally { setRestoringDatabase(false) } }
  async function rollbackDatabase() { if (!folder || !selectedSafetyBackup) return; if (!window.confirm(`Repor o estado anterior usando ${selectedSafetyBackup}? Esta operação substitui os dados atuais pelo backup de segurança.`)) return; setRollingBack(true); try { setStatus(`A repor ${selectedSafetyBackup}…`); const result = await rollbackDatabaseRestore(folder, selectedSafetyBackup); setRestoreDiff(null); setRestorePreview(null); await refreshSafetyBackups(folder); setStatus(`Rollback concluído: ${result.restoredRows} registos restaurados a partir de ${result.safetyBackup}.`) } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível executar o rollback.') } finally { setRollingBack(false) } }
  async function exportPortableProject() { setArchiving(true); try { setStatus('A preparar arquivo completo do projeto…'); const blob = await createProjectArchive(); downloadProjectArchive(blob); setStatus('Arquivo do projeto criado. No Safari, guarde o ZIP no iCloud Drive, No meu iPhone ou no Mac e extraia-o quando quiser uma pasta física do projeto.') } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível criar o arquivo do projeto.') } finally { setArchiving(false) } }
  async function importPortableProject(file?: File) { if (!file) return; setImportingArchive(true); try { if (!file.name.toLowerCase().endsWith('.zip')) throw new Error('Selecione um arquivo ZIP do Performance OS.'); if (!window.confirm('Importar este projeto vai substituir os dados atuais. Crie um backup antes se quiser poder voltar atrás. Continuar?')) return; setStatus('A importar o projeto…'); await restoreProjectArchive(file); setStatus('Projeto importado com sucesso. Recarregue a aplicação para atualizar todas as vistas.') } catch (error) { setStatus(error instanceof Error ? error.message : 'Não foi possível importar o projeto.') } finally { setImportingArchive(false); if (archiveInputRef.current) archiveInputRef.current.value = '' } }

  return <main className="pageShell"><header className="pageHeader"><div><p className="eyebrow">SISTEMA</p><h1>Armazenamento</h1><p className="pageLead">Local-first: Chrome/Edge usam uma pasta real; Safari usa armazenamento local persistente e permite criar um arquivo ZIP portátil com a estrutura completa do projeto.</p></div></header><section className="panel"><div className="panelHeader"><div><h2>Armazenamento do projeto</h2><p>{restoring ? 'A verificar armazenamento guardado…' : status}</p></div><span className="status">{mode === 'filesystem' ? 'PASTA LOCAL' : mode === 'indexeddb' ? 'SAFARI · LOCAL' : 'LOCAL'}</span></div><div className="dataCards"><article className="dataCard"><strong>{folder && typeof folder === 'object' && 'name' in folder ? folder.name : 'Nenhum armazenamento selecionado'}</strong><small>{mode === 'indexeddb' ? 'Projeto persistente no armazenamento local do Safari.' : 'Ficheiros organizados dentro da pasta escolhida.'}</small></article><article className="dataCard"><strong>{lastBackup ?? 'Ainda não criado'}</strong><small>Último backup local da base de dados.</small></article></div><div className="buttonRow"><button className="primaryButton" onClick={chooseFolder} disabled={restoring}>Ativar armazenamento local</button><button className="secondaryButton" onClick={reconnectFolder} disabled={!folder || restoring}>Confirmar acesso</button><button className="secondaryButton" onClick={testStorage} disabled={!folder || testing || restoring}>{testing ? 'A testar…' : 'Testar leitura/escrita'}</button><button className="secondaryButton" onClick={createDatabaseBackup} disabled={!folder || backingUp || restoring}>{backingUp ? 'A criar backup…' : 'Criar backup da base de dados'}</button><button className="secondaryButton" onClick={verifyDatabaseBackup} disabled={!folder || verifyingBackup || restoring}>{verifyingBackup ? 'A verificar…' : 'Verificar backup'}</button><button className="secondaryButton" onClick={previewRestore} disabled={!folder || previewingRestore || restoring}>{previewingRestore ? 'A preparar…' : 'Pré-visualizar restauro'}</button><button className="secondaryButton" onClick={compareRestore} disabled={!folder || comparingRestore || restoring}>{comparingRestore ? 'A comparar…' : 'Comparar com dados atuais'}</button><button className="primaryButton" onClick={restoreDatabase} disabled={!folder || !restoreDiff || restoringDatabase || restoring}>{restoringDatabase ? 'A restaurar…' : 'Restaurar em modo seguro'}</button></div>{restorePreview && <div className="dataCards"><article className="dataCard"><strong>Pré-visualização do restauro</strong><small>{restorePreview.filename} · exportado em {new Date(restorePreview.exportedAt).toLocaleString('pt-PT')}</small><small>{restorePreview.totalRows} registos no backup. A base atual não foi alterada.</small></article></div>}{restoreDiff && <div className="dataCards"><article className="dataCard"><strong>Comparação segura — nenhuma alteração efetuada</strong><small>Atual: {restoreDiff.totalCurrent} · Backup: {restoreDiff.totalBackup}</small><small>Novos no backup: {restoreDiff.added} · Ausentes no backup: {restoreDiff.removed} · Alterados: {restoreDiff.changed}</small></article>{Object.entries(restoreDiff.tables).map(([table, diff]) => <article className="dataCard" key={table}><strong>{table}</strong><small>Atual {diff.current} · Backup {diff.backup} · +{diff.added} · -{diff.removed} · ~{diff.changed}</small></article>)}</div>}</section><section className="panel"><div className="panelHeader"><div><h2>Projeto portátil</h2><p>Esta é a solução para teres uma pasta física organizada também quando trabalhas no Safari.</p></div></div><div className="dataCards"><article className="dataCard"><strong>📦 Exportar projeto completo</strong><small>Cria um ZIP com README, manifesto, database e a estrutura de clubes, equipas, jogadores, épocas, competições, jogos, relatórios, backups e exports.</small><button className="primaryButton" onClick={() => void exportPortableProject()} disabled={archiving || restoring}>{archiving ? 'A criar arquivo…' : 'Exportar projeto ZIP'}</button></article><article className="dataCard"><strong>📂 Importar projeto completo</strong><small>Restaura um ZIP do Performance OS para o armazenamento local deste navegador.</small><input ref={archiveInputRef} type="file" accept=".zip,application/zip" onChange={(event) => void importPortableProject(event.target.files?.[0])} disabled={importingArchive || restoring}/>{importingArchive && <small>A importar…</small>}</article></div></section><section className="panel"><div className="panelHeader"><div><h2>Backups de segurança</h2><p>São criados automaticamente antes de cada restauro.</p></div></div>{safetyBackups.length === 0 ? <p>Nenhum backup de segurança disponível.</p> : <div className="buttonRow"><select value={selectedSafetyBackup} onChange={(event) => setSelectedSafetyBackup(event.target.value)} disabled={rollingBack || restoring} aria-label="Backup de segurança"><option value="">Selecionar backup</option>{safetyBackups.map((name) => <option key={name} value={name}>{name}</option>)}</select><button className="secondaryButton" onClick={rollbackDatabase} disabled={!selectedSafetyBackup || rollingBack || restoring}>{rollingBack ? 'A repor…' : 'Repor estado anterior'}</button></div>}</section><section className="panel"><div className="panelHeader"><div><h2>Estrutura do projeto</h2><p>A mesma estrutura lógica em todos os browsers; no Chrome/Edge é uma pasta real, no Safari é persistente localmente e pode ser materializada como ZIP.</p></div></div><div className="positionRow">{PROJECT_FOLDERS.map((name) => <span className="chip" key={name}>{name}</span>)}</div></section></main>
}
